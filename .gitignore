# Third-party icon/font archives kept as local reference only, not versioned in this repo.
docs/lucide-icons/
docs/lucide-icons.zip
docs/lucide-font.zip
docs/lucide-font/

# Illustrator lock/autosave log files.
works/**/*.log

# Throwaway UI prototype (mattpocock-skills /prototype) for a Taylor Kanban product screen,
# not an Everline candidate component — kept locally as design reference, not versioned here.
works/html/taylor-agent-handoff-prototype/
