# Everline 狀態

最後更新：2026-08-04

本文件只保留「現在是什麼狀態」與仍然有效的背景事實，不做逐日流水帳——詳細的修正過程、逐步推理與驗證數據留在對應的 git commit 與 PR 說明裡，此處只摘要結論。定期回來把已經沉澱為背景事實或已被後續內容取代的條目收斂掉，避免這份文件本身變成又一個過期文件。

## 收尾狀態

- 狀態：`in-progress`
- 原因：目前已有可辨識的設計來源與預覽，但元件規格、審核狀態與 Taylor Kanban 技術整合仍未完成；第三批（8 類）進行中，細節見下方累計進度。
- 累計進度：第一批 17 類（`works/everline-components-master.svg`）、第二批 7 類、第三批 4 類（issue #5-#8，已畢業為 `works/html/batch3/`）、第四批 4 類（issue #9 Slider、#10 Accordion、#11 Popover、#12 Date / Time picker，已畢業為 `works/html/batch4/`）皆已有 candidate；四批合計 32 類皆仍是 candidate／草稿狀態，尚未逐項確認用途、狀態、無障礙、來源追溯等完整規格條件（見 `AGENTS.md` 元件規格的 12 項完成檢查），非正式定稿。第二階段候選（19 類，非必做）尚未開始。
- 2026-07-30：issue #17（第一批 15 群畢業為 HTML/CSS）依 issue 建議先試辦 Button、Checkbox、Text input／Textarea 三群（1/4），**2026-07-30 通過人類審查閘門（G1），已畢業，仍是 candidate，未宣告 stable**。P0 token 完整性核對對照 `works/everline-components-master.svg` 逐項數值後，補上 4 個先前未涵蓋的缺口（純新增，未改動任何既有解析值，見 `tokens/everline-draft.tokens.json` 對應 `$description`）：Button 的 neutral／outline 變體文字色其實是 off-white 而非既有 `foreground` token 所指的 white；Checkbox 未勾選狀態的 1px 邊框先前未被任何 token 涵蓋；Text input／Textarea 的 disabled 前景色與 Textarea 的 foreground／placeholder／focus 邊框先前完全沒有對應 token。候選規格位於 `works/html/batch1/`，以 `node works/html/batch1/verify.mjs`（CDP 驅動真實 headless Chrome）驗證。PR #29 提出後，codex 合併前審查發現 3 個 Major、1 個 Minor，已於 PR head 之後一輪修正：(1) disabled Button 誤對整顆元件套 `opacity`，把核准 SVG 的實色 `#666666` 背景一起壓暗，已改為只對前景文字用既有 `color-mix()` 技法混色，背景維持實色；(2) `component.checkbox.indicator-size`（20px）與 CSS 同名 custom property 被誤標成 32px，驗證腳本先前只比對「custom property 有無對應到某個 token 路徑」、未比對解析值，已改用正確的 `component.checkbox.size` 作為 mask 畫布，並把 CSS 契約驗證升級為同時比對解析值；(3) checkbox 命中區只有 32×32px，未達規格「32px 視覺置於至少 48×48px 命中區」的要求，已將原生 `<input>` 本體擴到 48×48px、透過 `::before`／`::after` 偽元素在其中置中繪出 32×32px 視覺，label 關聯與 indeterminate 真實 DOM 狀態不受影響；(4/Minor) `component.textarea.foreground` 的 token 描述先前誤寫成「依 SVG 量測」，實際上 SVG 只畫了 placeholder 樣式文字、沒有可查證的 filled 範例，已改記為 G1 接受的延伸決定。`works/everline-components-master.svg` 的 c-button／c-checkbox／c-text-input／c-textarea 四個 SVG 群組已標記群組級 `superseded-by: works/html/batch1/index.html`（master.svg 檔案層級狀態不變，仍是 candidate；除 c-color-swatch 外共 16 個產品元件群組，扣除這 4 個，其餘 12 個 SVG 群組不受影響）。Button 的 `pressed`／`loading`、欄位的 `error` 狀態，以及既有的 outline 變體與 focus ring 共用同一視覺的未決問題，仍未實作／未解決，留待後續審查決定。issue #17 其餘 12 群（15 群中未畢業的部分）不受影響，仍以 master SVG 為權威來源。修正後的 `verify.mjs` 新增了 custom property 解析值與 token 逐一比對（而不只是名稱比對）、disabled Button 的真實截圖 RGB 驗證，以及 checkbox 48×48 命中區的真實點擊測試，重新執行後 62/62 通過。
- 2026-07-31：issue #17 新增 Icon button（issue #30）、Switch（issue #31）、Radio（issue #32）、Split button／Dropdown（issue #33）四群候選規格，**candidate ready for G1，尚未通過人類視覺審查**；`works/everline-components-master.svg` 對應的 `c-icon-button`／`c-switch`／`c-radio`／`c-split-button` 群組尚未標記 `superseded-by`，master SVG 仍是這 4 群目前的權威來源，issue #30–#33 亦未打勾關閉。P0 token 完整性核對對照 SVG 逐項數值後：Icon button 補上 `foreground`／`foreground-neutral`／`foreground-disabled`／`border-width`／`border-focus`（與 Button 相同的既有缺口模式，純新增）；Radio 補上 `border`／`border-width`（與 Checkbox 相同的既有缺口模式）；Split button 補上 `foreground`（white）、`menu-foreground`（off-white，比照 Popover 既有慣例明確宣告）、`divider-color`（`#4A76C9`，SVG 已畫出但從未 token 化的既有分隔線顏色，非新設計）、`layer`（alias `layer.overlay`）；Switch 核對後沒有發現任何缺口，既有 token 已完整涵蓋 on／off／disabled／danger-on 四個已畫出狀態。候選規格擴充至 `works/html/batch1/`：Icon button 用真實 SVG 圖示（沿用 batch 3 已建立的 icon 慣例）取代 master SVG 的「圖示」文字佔位；Switch 用原生 `<input type="checkbox" role="switch">`，accessible checked 狀態由瀏覽器依真實 `checked` 屬性自動推導，另補 Enter 鍵切換（native checkbox 原生不支援，click／Space 為原生行為）；Radio 用同 `name` 的原生 radio 群組包在 `<fieldset>`／`<legend>` 內，方向鍵選擇與 Tab 整組進出皆為瀏覽器原生行為，未寫額外 JS；Split button 的主動作與 disclosure 為兩個獨立 `<button>`，選單是真實 `role="menu"`／`role="menuitem"` 搭配 roving `tabindex`，`ArrowDown`／`Enter`／`Space` 開啟、方向鍵在選單內移動、`Enter` 執行並將焦點還給 disclosure、`Escape` 與點擊選單外皆可關閉；選單項目本身的 `hover`／`selected`／`disabled`／`loading` 狀態，以及 Switch 的 `focused`／`loading`／`error`、Radio 的 `error group`、Icon button 的 `pressed`／`selected`／`tooltip`（及與 Button 相同的 outline／focus-ring 共用同一視覺的既有未決問題）SVG 皆未畫出，本輪不實作，列為 G1 決策清單；選單項目的鍵盤 focus 例外——它們是真實 `<button>`，全域 `:focus-visible` 本來就會套用，只是沒有另外定義 menu item 專屬的 focus 視覺，並非完全沒有回饋。動畫：Split button 的 SVG 註記「needs transition animation」，因 motion token 仍是未驗證 candidate，比照 batch 4 Accordion／Popover 的既有決定不加動畫。以 `node works/html/batch1/verify.mjs`（CDP 驅動真實輸入事件，涵蓋 token 完整性、CSS 解析值契約、computed dimensions、真實點擊與鍵盤操作、RGB 截圖採樣、響應式無溢出、無動效掃描）驗證，125/125 通過。
- 2026-08-03：issue #17 新增 Badge / Tag（issue #34，15 群清單第 8 項）候選規格，**candidate ready for G1，尚未通過人類視覺審查**；`works/everline-components-master.svg` 對應的 `c-badge-tag` 群組尚未標記 `superseded-by`，master SVG 仍是這一群目前的權威來源，issue #34 亦未打勾關閉，不使用 `Closes #17`。此元件先前完全沒有 token 涵蓋，P0 核對後新增 18 個 `component.tag.*` 欄位（純新增，非缺口修正，見 `tokens/everline-draft.tokens.json` 對應 `$description`；其中 `remove-icon-offset` 是 PR #46 複審過程中新增，非最初一次到位），皆逐一對照 `c-badge-tag` SVG 核實：neutral／accent／danger／disabled 四色沿用既有語意 token（`background-surface`／`action-primary`／`action-danger`／`background-subdued`），文字與圖示前景延續 Button／Icon button 已確立的「primary／danger 變體用 white，neutral／outline 變體用 off-white」規則；圓角（20px，40px 高度之半）、選取圓點（8px 直徑）為新宣告字面值；命中區相關的 `hit-area-size`（48px，outline 整體可互動篩選標籤的命中區）與 `remove-control-size`（32px，removable 移除鈕）皆非 SVG 直接畫出，是依規格文字與既有 Popover／Breadcrumb 命中區慣例做的延伸決定。候選規格擴充至 `works/html/batch1/`：outline 變體實作為真實 `<button aria-pressed>`，滿足規格「可點擊篩選器需明確游標、鍵盤操作及選取狀態」的互動要求，但 SVG 只畫出一種 outline 外觀、從未畫出選中／按下的獨立視覺，故切換 `aria-pressed` 時外觀不變（與 Button 未畫出的 `pressed`／`loading` 同一處理原則，不自行發明視覺）；removable 變體的移除鈕是獨立的真實 `<button>`，有專屬 `aria-label="移除：<標籤文字>"`，點擊會將該 tag 從畫面移除（真實 DOM 異動，非只是淡出，因 SVG 未畫出移除動畫）；leading 圓點與兩個圖示皆使用 `currentColor`，未另立顏色 token，因為 SVG 本身的圓點／圖示填色與同一個 tag 的文字填色一律相同。以 `node works/html/batch1/verify.mjs` 驗證，涵蓋 token 完整性、CSS 解析值契約、computed dimensions、outline 的真實點擊與鍵盤（Enter／Space）切換測試、removable 移除鈕的真實點擊測試（測試後於同一頁面重新插入並重新綁定監聽器，恢復頁面狀態供 G1 審查截圖使用）、RGB 截圖採樣，165/165 通過（含既有 7 群的既有檢查，全部重新執行仍通過）。PR #46 提出後，opus 審查發現並已修正：(1) `component.tag.hit-area-size`／`remove-control-size` 的 token `$description` 與 `verify.mjs` check 標籤誤寫成「G1-accepted extension decision」，但 Badge/Tag 尚未通過 G1，與同一份 PR 的其餘文件自相矛盾，已改為「extension pending G1 review」一類措辭；(2) removable 移除鈕的鍵盤 Enter 觸發移除後，被刪元素連同鍵盤焦點一起消失、掉回 `<body>`，屬真實鍵盤導覽缺陷（不受「SVG 未畫出視覺不得發明」保護），已比照 batch 4 Popover「focus 一律要回到明確位置」的既有先例修正：改為移到同群組下一個／上一個移除鈕，皆不存在時退回群組容器本身（`.tag-examples` 補上 `tabindex="-1"` 作為此退回目標），並在 `verify.mjs` 新增 `document.activeElement` 斷言；(3) `verify.mjs` 的「Rendered colour」區塊先前沒有任何 Badge/Tag 條目，本輪四個新實色（neutral／accent／danger／disabled 背景）只做了 `getComputedStyle` 沒有真實截圖 RGB 採樣，不符本檔案既有慣例（Button／Icon button／Switch 新增實色時皆有採樣，disabled Button 當初就是靠這個方法抓出 PR #29 的背景誤變暗問題），已比照既有手法補上。以下 3 點審查核實無誤、屬設計決策層級的問題，2026-08-03 由使用者本人當場拍板解決（不留到之後的 G1 才決定）：(a) outline 靜態邊框與全域 `:focus-visible` 焦點環同色同寬的疑慮——以真實鍵盤 `Tab` 導覽並截圖核對後，48px 外層命中區的焦點框與 40px 內層 pill 自己的邊框在視覺上是兩個可分辨的獨立形狀（矩形外框＋膠囊內框），維持現狀不修改；`component.tag.border-focus` 的命名與數值皆維持不變——數值是 SVG 本身畫的顏色不可為了改名連帶質疑，命名雖與 Text input／Textarea 不同語意但與 `component.button.border-focus`／`component.icon-button.border-focus`（Button 這組已通過實際 G1 審查）完全一致，只改 Tag 一個反而製造新的不一致。(b) disabled 元素類型——改為 `<span class="tag tag--disabled" aria-disabled="true">`，不再用 `<button disabled>`，與其餘五個純狀態變體一致，`verify.mjs` 改用 `aria-disabled` 屬性驗證。(c) removable 移除鈕圖示位置——新增 `component.tag.remove-icon-offset`（23px，逐一核對 SVG `tag-removable` X 圖示中心與 rect 右緣量測值 144－121）、removable 自己的 `padding-inline-end` 改用 `calc(remove-icon-offset − remove-control-size ÷ 2)`（7px）取代共用的 16px，讓圖示中心恢復到與 SVG 一致的 23px，同時完整保留 32px 命中區；`verify.mjs` 新增真實 `getBoundingClientRect` 量測斷言（23px），不只信任 CSS 宣告值。三項決策已同步寫入 `works/html/batch1/index.html` 對應 scope note。全部修正後重新執行 `node works/html/batch1/verify.mjs`，172/172 通過。PR #46 第三輪審查（opus）另外找到兩點並已修正：(1) 新增 `remove-icon-offset` 後 `component.tag.*` 實際欄位數已是 18，`docs/STATUS.md`／`works/html/batch1/index.html` 卻還寫著 17，與 PR body 已更新的數字互相矛盾，已一併訂正為 18；(2) disabled 改成 `<span>` 後雖滿足「不可互動」的規格要求，但停用態與 neutral 的視覺差異僅剩背景色與文字透明度（皆為顏色），違反規格「不得只依顏色傳達停用」「需要文字、圖示或上下文共同說明」的既有條文——此視覺落差本身在改動前就存在，非本輪造成，但 `<button disabled>` 至少會被輔助科技播報，改為不可聚焦的 `<span>` 後這條路徑實質消失，屬本輪 `<span>` 決策新造成的程式化傳達退步；已在 disabled 範例內加入沿用既有 `.visually-hidden` utility 的「（已停用）」文字（螢幕閱讀器可讀、肉眼不可見，不算發明新視覺），`verify.mjs` 新增對應斷言。全部修正後重新執行，174/174 通過。
- 2026-08-04：issue #17 新增 Inline alert（issue #35，15 群清單第 9 項）候選規格，**2026-08-04 通過人類 G1 視覺審查，已畢業，仍是 candidate，未宣告 stable**；`works/everline-components-master.svg` 對應的 `c-inline-alert` 群組已標記群組級 `superseded-by: works/html/batch1/index.html`（master.svg 檔案層級狀態不變），issue #35 仍未打勾關閉（G1 通過與 GitHub issue 關閉是兩件事，本輪未對 GitHub 做任何開關動作），不使用 `Closes #17`。此元件先前完全沒有 token 涵蓋，P0 核對後新增 `component.inline-alert.*` 欄位（純新增，非缺口修正；PR #47 審查過程中增減後定案為 21 個，詳見下方修正紀錄），逐一對照 `c-inline-alert` SVG 核實，過程中發現並修正 3 處與既有草稿文字的落差：(1) `docs/design-system-v0.1-draft.md` 原寫「24 px 狀態圖示」，SVG 實測是 `circle r="14"`（直徑 28px，與 `component.tag.dot-size` 相同的「直徑＝2×半徑」量測法），已依實測值修正為 28px 並同步訂正該文件；(2) `alert-neutral` 的指示條（`fill="#666"`，`background-subdued`）與圖示線條（`stroke="#f2f2f2"`，`foreground-primary`）並非同一顏色——info／danger 兩者指示條與圖示同色，唯獨 neutral 例外，已拆成 `rail-color-neutral`／`icon-color-neutral` 兩個獨立 token；(3) SVG 樣式表的 `.alert-title{fill:#f2f2f2}` 與 `.alert-body{fill:#b8b8b8}` 是兩種不同文字色，後者不符合既有任何 token，新增 `color.base.gray-500`（沿用 `gray-850` 當初「直接照 SVG 加值，不硬套進既有角色」的先例），並拆成 `component.inline-alert.foreground-title`／`foreground-body`；同批也發現 `.alert-title` 字級 17px 不在既有 `font.size-label`(14)／`size-body`(16)／`size-title`(24) 任何一階，新增 `title-size` 字面值 token，`.alert-body` 14px 則剛好對上既有 `font.size-label`，以 `body-size` 別名沿用。`dismiss-control-size`（48px，關閉鈕命中區）非 SVG 直接畫出——master.svg 重繪時拿掉了舊來源檔 `works/everline-agent-components-v0.1.svg` 原有的 48×48 透明命中區 rect，本輪依本地座標換算（712–760 × 24–72，剛好貼齊卡片右邊界）與既有 `size.control-md` 慣例還原，屬**延伸決定，尚待 G1 確認**，理由同 Badge/Tag 的 `remove-control-size`。候選規格新增至 `works/html/batch1/`：三個變體（info／danger／neutral），只有 info 範例畫出關閉鈕，danger／neutral 未畫出故不新增；info／neutral 使用 `role="status"`，danger 用 `role="alert"`（直接依 `docs/design-system-v0.1-draft.md` 既有文字，非發明）；容器用 `min-height` 而非固定 `height`，讓文字換行時可撐高而不溢出，屬工程健壯性而非新視覺；rail／icon 的絕對定位座標皆以 `calc()` 從 `rail-inset`／`rail-width`／`content-start`／`icon-size` 四個 token 推導，未新增未記錄的原始值；關閉鈕點擊會將整個 alert 從畫面移除（真實 DOM 異動，因 SVG 未畫出移除動畫），比照 Badge/Tag `removable` 的既有先例，移除後 focus 移至群組容器（`tabindex="-1"`）而非掉回 `<body>`；沒有 success／warning 變體（docs 草稿已明文使用者確認前不自行加入）；SVG 未畫出任何操作按鈕，本輪不新增。以 `node works/html/batch1/verify.mjs` 驗證，涵蓋 token 完整性、CSS 解析值契約、computed dimensions、`role` 屬性斷言、關閉鈕真實點擊與 focus 回復測試、三變體 RGB 截圖採樣，初版 218/218 通過（含既有 7 群的既有檢查，全部重新執行仍通過）。PR #47 提出後，codex 審查發現 2 個 Major、2 個 Minor，已於 PR head 之後一輪修正：(1) 狀態圖示的 SVG `viewBox="0 0 32 32"`（32 個單位是為了同時容納較寬的 danger 三角形）被直接套進依 `icon-size`（28px）決定寬高的 CSS 容器，導致 viewBox 內每個單位一起被等比縮小，實際只渲染 24.5px，非宣稱的 28px；已新增 `component.inline-alert.icon-render-margin`（2px，純實作用的縮放安全邊界，非 SVG 量測值），圖示容器改為 `icon-size + icon-render-margin × 2`（32px）讓 viewBox 與渲染像素維持 1:1，圖示水平定位的 calc() 也一併扣掉這個邊界以維持視覺中心不變。(2) 關閉鈕的 X 圖示中心距卡片右緣實際是 48px，而非 SVG 實測的 24px——容器原本無條件套用的 `padding-inline-end: 24px`（給沒有關閉鈕的變體留白用）疊加在關閉鈕自己 48px 寬置中的效果之上，與 PR #46 當初 Badge/Tag 移除鈕圖示位移是同一種錯誤形狀（命中區疊加在既有內距之上而非取代它）；X 圖示路徑本身也是直接複製 Tag 移除鈕的路徑、未依來源重新量測，只有 8px 寬（來源是 12px）。已新增 `.inline-alert:has(.inline-alert__dismiss) { padding-inline-end: 0 }` 規則（24px 期望位移剛好等於 48px 按鈕寬度的一半，歸零後按鈕貼齊卡片右緣即可自然置中對到 24px），並重畫 X 路徑為 12 個單位。(3/Minor) `component.inline-alert.border-width`／`border-focus` 兩個 token 只在 `:root` 宣告、從未被任何 CSS 規則消費——關閉鈕的 focus ring 其實完全由既有跨元件的全域 `:focus-visible` 規則滿足規格文字要求，已直接移除這兩個 token（tokens／CSS／`verify.mjs` 三處同步），最終 `component.inline-alert.*` 定案為 21 個欄位。(4/Minor) `index.html` 的 `<title>`與 `verify.mjs` 檔頭註解仍寫著「等待 G1」，與其他文件已同步的「已畢業」狀態矛盾，已一併訂正。`verify.mjs` 新增了對 `<circle>` 與 X `<path>` 元素本身（而非其外層容器）的真實 `getBoundingClientRect` 幾何斷言，不再只信任容器宣告尺寸；全部修正後重新執行，220/220 通過，codex 複審確認可以合併。
- 2026-08-04：issue #17 新增 Menu / Context menu（issue #37，15 群清單第 11 項）候選規格，**candidate ready for G1，尚未通過人類視覺審查**；`works/everline-components-master.svg` 對應的 `c-menu` 群組尚未標記 `superseded-by`，master SVG 仍是這一群目前的權威來源，issue #37 亦未打勾關閉，不使用 `Closes #17`。`component.menu.*` 早在 2026-07-19 就先行宣告了 10 個欄位（row-height、radius、item-radius、padding、background、hover-background、divider、danger-foreground、disabled-foreground、checked-indicator、shortcut-foreground），但從未對照 `c-menu` 實際核實過；本輪 P0 核對後發現 8 個缺口，皆為純新增：`item-padding-inline`（12px，逐一量測每個 item 文字與捷徑文字的左右內距，不落在既有 8px 級距上）、`border`／`border-width`（面板本身的 `stroke="#444"` 描邊，先前一個字都沒有涵蓋）、`item-focus`／`item-focus-width`（「keyboard focus item」範例自己畫出的 2px 藍色 ring——這是本批目前唯一一個明確畫出專屬鍵盤 focus 視覺的元件，其餘元件皆沿用全域 `:focus-visible`，故本元件的 item 改為覆蓋掉全域樣式、改用這組專屬 token）、`checked-icon-inset`（4px，checked item 打勾圖示比其餘 item 的 12px 文字內距更靠近邊緣的量測光學內縮，比照 `component.switch.thumb-inset` 既有先例）、`shortcut-font-size`（12px，捷徑提示文字沿用 SVG 樣式表 `.caption` 的字級，不落在既有 `font.size-*` 任何一階，比照 `component.inline-alert.title-size` 既有先例）。候選規格新增至 `works/html/batch1/`：真實 `role="menu"` 面板搭配 `role="menuitem"`／`role="menuitemcheckbox"` 的 `<button>` 與 roving `tabindex`（與 Split button 選單相同做法），觸發鈕重用既有 Icon button（依 SVG 註記「trigger reuses Icon button / Button」）；`ArrowDown`／`Enter`／`Space` 開啟並移入第一個可用項、方向鍵移動並略過 disabled 項（真實鍵盤測試涵蓋略過 disabled 項並正確 wrap）、`Enter` 執行並關閉、`Escape` 關閉並將焦點還給觸發鈕（依規格文字「關閉後焦點必須明確返回觸發元件」），但 `Tab` 離開與點擊選單外的 light-dismiss 刻意不搶焦點——比照 Split button 選單既有的同一個判斷（使用者主動把焦點移到別處時，強行拉回觸發鈕才是真正的搶焦點行為，「不可遺失」指的是不能掉到 `<body>`，這兩條路徑本來就會落在真實明確的元素上）；`role="menuitemcheckbox"` 的 checked item 真實可切換（點擊或 `Enter`／`Space` 會真的翻轉 `aria-checked` 與圖示顯示，即使 SVG 只畫出「已勾選」一種外觀，未勾選時也只隱藏圖示、不發明額外視覺）；捷徑提示文字（「⌘G」「Esc」）僅為視覺展示，本批未綁定實際全域快捷鍵。submenu 指示 SVG 未畫出，本批不新增。開發過程中 `verify.mjs` 自測抓到並修正 1 個真實 bug：`.menu__panel` 一開始直接寫了 `min-width: 220px` 當成元件規則本身的一部分，違反本檔案自己「元件規則不得含原始 px 值」的既有契約（`verify.mjs` 的 CSS 契約檢查當場抓到）——改參照 Text input／Select 既有「無寬度 token，寬度由容器決定」的先例，把這個 220px 移到只在 `.menu-examples` 目錄範例層級的選擇器裡，不算元件契約值。以 `node works/html/batch1/verify.mjs` 驗證，涵蓋 token 完整性、CSS 解析值契約、computed dimensions、真實點擊與鍵盤操作（含專屬 focus ring 的 `:focus-visible` 驗證——需在真實鍵盤事件之後才會觸發，不能緊接在滑鼠點擊開啟選單之後檢查，這點在撰寫測試時也曾一度寫錯並修正）、`role`／`aria-*` 屬性斷言、RGB 截圖採樣、響應式無溢出、無動效掃描，252/252 通過（含既有 10 群的既有檢查，全部重新執行仍通過）。
- 2026-07-30：第四批（issue #9 Slider、#10 Accordion、#11 Popover、#12 Date / Time picker）依 `batch4-plan.tmp.md` 決策獨立成批。SVG 候選稿當日經人工審查通過（審查中決定兩項視覺修正：Slider 焦點環改為 4px 實心無間隙、日期範圍中段改為每日獨立淡色圓而非連續色帶），隨後完成 token 核對與 HTML/CSS 畢業。`works/everline-components-batch4.svg` 已標記為歷史快照，候選規格權威來源為 `works/html/batch4/index.html`，規格與驗證紀錄見 `docs/design-system-v0.1-batch4-draft.md`。四類皆為 candidate，G1–G6 已通過人類驗收，仍非 stable。驗證以 CDP 送真實輸入事件執行（合成事件無法觸發原生 popover 的 Esc／light-dismiss）並含 RGB 採樣與全頁無動效掃描。過程中修正的真實缺陷：值標籤間距公式、時間欄位被 flex 壓縮導致原生地區化時間值裁切（token 128px 不足，改 160px）、矮視窗下 Popover 溢出畫面，以及 PR 審查後修正的月底 PageUp／PageDown 溢位與焦點回復對象錯誤。
- 2026-07-28：Breadcrumb（issue #7）SVG 候選稿經一輪人工審查修正——原始靠左排列改為每項文字置中於自己的命中區（以 Chrome `getBBox()` 實測文字寬度，非目測），並修正 Loading／Search field 兩區塊間距過窄（原僅 18px，覆蓋到下一區塊標題）的既有排版問題。畢業到 HTML/CSS 時，`<li>` 使用 `display: contents` 讓子元素直接參與 flex 版面，命中區置中改由原生 padding 自然達成，不需複製 SVG 手算的 slot 座標；過程中發現並修正一個真實 bug：`display: contents` 在同優先權下會蓋掉瀏覽器預設的 `[hidden]{display:none}`，導致 collapsed 範例的摺疊項目一開始就沒被正確隱藏（與本檔案既有 `.loading-overlay[hidden]` 註解記載的同一類陷阱）。Number input（issue #8）延伸 Text input／Icon button 既有 token，畢業時改用原生 `<input type="number">` 搭配 `stepUp()`/`stepDown()`，min/max 到界時對應按鈕即時原生 `disabled`（點擊或直接輸入到界皆會觸發）。兩者的像素級驗證（computed style 讀值＋螢幕截圖 RGB 採樣＋DOM 尺寸量測，2026-07-29 PR #26 審查修正後於 PR head 重新完整量測）：Breadcrumb——item 間 32px slot（8px+16px chevron+8px，皆由 `component.breadcrumb.item-gap` 推導，不再借用 `item-padding-inline`）、命中區高 32px、chevron 16px、collapsed 省略號 3 個 4px 圓點、中心距 10px（原 CSS 誤用 3px flex-gap 只達 7px 中心距，已修正為 `calc(ellipsis-dot-gap - ellipsis-dot-size)`）、預設文字色 `rgb(77,77,77)`＝`#4d4d4d`、目前頁／hover 文字色 `rgb(242,242,242)`＝`#f2f2f2`、hover 底色 `rgba(255,255,255,0.06)`。Number input——按鈕 48×48px、欄位 80×48px、按鈕與欄位間 8px、按鈕圖示 24px（原 HTML 圖示 `width/height` 寫死 24，未消費 `button-icon-size` token，已改由 CSS 控制、SVG 移除硬編碼尺寸）；focus 邊框 token 解析為 2px／`#598AE8`，因本輪瀏覽器自動化工具的 `element.focus()` 未取得真正的視窗層級 focus（`document.activeElement` 為欄位本身，但 `:focus` 選擇器不匹配），改以手動套用相同 `box-shadow` 值截圖核對渲染結果，未能直接觸發 `:focus` 偽類驗證，屬已知剩餘風險（此為既有 Text input／Search field 已審查通過的相同技法，非本輪新增邏輯）；default 欄位背景 `rgb(102,102,102)`＝`#666666`、focus 邊框 `rgb(89,138,232)`＝`#598AE8`；min／max reached 以點擊、直接輸入（原生 `input` 事件模擬）分別測試邊界，對應按鈕即時 `disabled`、另一按鈕維持可互動；disabled 狀態背景實測仍為 `rgb(102,102,102)`＝`#666666`（維持實色，未變暗）、內容色實測 `color(srgb .702 .702 .702)`≈`rgb(179,179,179)`＝`#b3b3b3`，與「off-white + opacity.disabled 0.55」的理論混色值一致（原 CSS 對整個 button/input 套用 `opacity: .55`，導致背景也被錯誤帶淡至約 `#444`、內容約 `#919`，已改為背景維持 token 實色、只用 `color-mix()` 對前景色套用 disabled 混色，不再影響背景）。方向鍵（↑/↓）路徑因瀏覽器自動化工具本輪不支援真實鍵盤事件模擬，改以程式碼路徑等價性驗證：鍵盤與點擊、輸入共用同一個原生 `input` 事件觸發的 `sync()`，點擊與直接輸入兩條路徑皆已實測通過，鍵盤觸發的瀏覽器原生 step 行為本身不受本次改動影響，未逐一以真實鍵盤事件覆蓋，屬已知剩餘風險。
- 2026-07-27：Progress/Loading 元件同時回填了第二批 List／Data table／Kanban column 先前擱置的 `loading` 狀態（`works/html/batch2/`），沿用同一 spinner 視覺套進既有 `empty-state` 容器，未另立新視覺語言；過程中順手修正了 batch2 既有的全域 `prefers-reduced-motion` 規則對「持續循環動畫」（spinner 旋轉）不適用的問題（原規則會讓動畫變成極速閃爍，而不是正確地停在靜態畫面）。
- 2026-07-27：Search field（issue #6）延伸既有 Text input 的 token 與結構（高度、radius、背景、placeholder 色、focus 邊框皆直接沿用），只新增 `component.search-field` 這一組 icon／清除按鈕的版面 token，沒有新增顏色或動效原生值。畢業到 HTML/CSS 時把 SVG 候選稿手繪的 empty／filled／focused／disabled 四張快照，換成原生 `:focus-within`（focus）與 `input` 事件驅動的清除按鈕出現／隱藏、原生 `:disabled`／`:has()`（disabled），而不是逐一手刻對應狀態的 CSS class。像素級驗證（computed style + screenshot RGB 採樣）：背景 `rgb(102,102,102)`＝`#666666`、focus 邊框 `rgb(89,138,232)`＝`#598AE8`、disabled 內容色 實測 ≈`rgb(178,178,178)` 與「off-white + opacity.disabled 0.55」的理論混色值（179,179,179）一致，全部與 token 精確吻合。
- 待辦、規劃與跨元件擱置決策一律追蹤在 GitHub Issues（`gh issue list`），不在此文件重複列。

- 2026-08-14：Menu / Context menu（issue #37）人類 G1 視覺審查通過，已依既有畢業流程將 master.svg 的 `c-menu` 標記群組級 `superseded-by: works/html/batch1/index.html`；HTML/CSS 現為候選規格權威來源，仍未宣告 stable。未改動 Menu 的視覺、語意、keyboard 行為或 issue 狀態。

## 背景事實

- 專案目前以設計語言、GUI 元件參考與向量原始檔管理為主，尚未進入前端元件庫實作；本專案沒有可執行的測試、lint 或 build 流程，驗證方式是 headless Chrome 渲染＋像素核對，不是自動化測試（`docs/spec.md` 已同步移除誤導性的「Test Plan」用詞，詳見該文件 Open Questions 前一節）。
- 主要規格入口：`docs/spec.md`；元件研究參考（非正式規格）：`references/gui-components-reference.md`。
- 向量原始來源：`works/illustrator/everline_p1.ai`（另有色票來源 `everline.ai`、結構參考 `TaylorConceptMockup1.ai`，皆放在 `works/illustrator/`，此類工作檔命名不受資產命名規則約束）。
- UI 字型已確認為 Noto Sans TC（原始 SVG 文字已轉外框，無法由檔案本身驗證，2026-07-19 由使用者直接確認，不再是待確認項目）。
- 候選規格與 token：`docs/design-system-v0.1-draft.md`（第一批）／`docs/design-system-v0.1-batch2-draft.md`（第二批）、`tokens/everline-draft.tokens.json`。`works/everline-agent-components-v0.1.svg` 已由 master 總表取代，標記 `superseded-by`，保留為歷史快照。
- HTML/CSS 候選流程：Illustrator 構思 → SVG 候選＋人類審查 → 確認後轉 `works/html/` 的 HTML/CSS（「畢業」，該元件之後以 HTML/CSS 為權威來源，SVG 標記歷史快照，不再更新）。完整規則見 `AGENTS.md`，含畢業前的 token 完整性核對與畢業後的像素級驗證要求（2026-07-23 因批次二的多起 token／CSS 落差新增）。
- 第二批畢業後歷經多輪審查與修正（欄寬 token 誤植、Kanban 欄位與卡片背景同色、`limit-reached` 內容與徽章矛盾、跨元件擱置決策拖曳／empty／分頁／resizing、`collapsed` 狀態實作後確認非需求而撤銷、`foreground-subdued` 等 8 個 token/CSS 落差），完整過程見 PR #4、#16、#18、#19 與對應 commit。教訓：批次畢業或處理舊 backlog 項目前，都要先跟已審查來源或使用者重新確認，不能把候選值或舊擱置清單直接當成已核准事實執行。
- `docs/TODO.md`、`docs/NEXT_ACTION.md`、`docs/HANDOFF.md` 已刪除（2026-07-23～24），元件待辦與下一步規劃改用 GitHub Issues，快速接手改讀本文件＋`AGENTS.md`＋`docs/spec.md`。`docs/spec.md` 的「工程防護層」也已精簡，拿掉套用軟體工程規格範本但實際上無法執行的 Contract／Acceptance Criteria／Test Plan／Drift Risk Analysis，只留 FROZEN Decisions 與 Open Questions。
- `.gitignore` 已排除第三方 Lucide 素材（`docs/lucide-icons/`、`docs/lucide-icons.zip`、`docs/lucide-font.zip`，新環境 clone 不會取得，需要時自行從上游重新下載）與 Illustrator log／鎖定檔（`works/**/*.log`）。

## 已確認觀察

- 多數 UI 元素具備重複使用價值，後續適合整理為正式元件規格，而不是只停留在單次畫面示意。
- 已出現可共用的狀態模式：default、disabled、danger、outline、on/off、focused。
- 目前畫面偏向深色工作型工具介面，適合延伸成 Taylor 系列產品共用的設計語言。

## 已知限制

- Taylor Kanban repo 與實際技術結構仍未驗證，不應把前端實作細節寫成事實。
- `works/illustrator/everline_p1.ai` 的內部圖層、元件命名與可重用結構尚未文件化；GitHub Issue #14 曾嘗試追蹤此事，已確認回填圖層命名需要人工在 Illustrator 裡操作、不是 agent 能做的事，故已結案，此限制目前無對應追蹤項目。
- 已有候選 design token 與兩批完整 component 草稿，但尚未建立正式穩定 schema、所有互動狀態的視覺稿或 assets index。
- `works/everline-components-master.svg` 中既有元件是依已驗證量測值重建，非逐位元組複製原始檔案；第一批新增的 6 類元件僅畫出核心狀態，完整互動／無障礙狀態仍待補齊。

## 未追蹤本機工作

- `works/TaylorConceptMockup1.svg`（Taylor 主視窗結構概念圖，`works/illustrator/TaylorConceptMockup1.ai` 的匯出）僅作結構參考，非 Everline 樣式來源。
